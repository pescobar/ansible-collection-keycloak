---
# Internal vars (not intended to be overridden by users)

# Absolute paths to the rendered artifacts.
_keycloak_deploy_compose_file: "{{ keycloak_deploy_dir }}/docker-compose.yml"
_keycloak_deploy_env_file: "{{ keycloak_deploy_dir }}/.env"
_keycloak_deploy_caddyfile: "{{ keycloak_deploy_dir }}/Caddyfile"
