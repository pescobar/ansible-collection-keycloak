---
# Internal vars (not intended to be overridden by users)

# Absolute paths to the rendered artifacts.
_keycloak_compose_file: "{{ keycloak_deploy_dir }}/docker-compose.yml"
_keycloak_env_file: "{{ keycloak_deploy_dir }}/.env"
_keycloak_caddyfile: "{{ keycloak_deploy_dir }}/Caddyfile"
